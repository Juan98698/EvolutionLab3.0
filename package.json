{
  "name": "evolution-lab",
  "version": "1.0.0",
  "scripts": {
    "build": "shx rm -rf src_temp public && shx mkdir -p src_temp public clientes && shx cp index.html login.html src_temp/ && shx cp fondo.jpg manifest.json sw.js icon.png icon-192.png public/ && shx cp -r clientes public/ && html-minifier-terser --input-dir src_temp --output-dir public --file-ext html --collapse-whitespace --remove-comments --minify-css true --minify-js true"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "shx": "^0.4.0"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
